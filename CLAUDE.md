@AGENTS.md

# Git

- Never add `Co-Authored-By: Claude` or any Claude attribution to commit messages.

## Session Rules

- **Caveman mode always active** — use `/caveman` skill (full level) every session, every response
